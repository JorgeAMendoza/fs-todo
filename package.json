{
  "devDependencies": {
    "husky": "^8.0.3",
    "lint-staged": "^15.0.2"
  },
  "scripts": {
    "prepare": "husky install",
    "docker:dev:start": "docker-compose -f server/docker-compose.dev.yml up",
    "docker:dev:down": "docker-compose -f server/docker-compose.dev.yml down"
  }
}
